<h3> <%= @order.customer.family_name %><%= @order.customer.first_name %>さんの注文履歴</h3>


<div class="container">
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>購入日時</th>
        <th>注文個数</th>
        <th>注文ステータス</th>
      </tr>
    </thead>
    <tbody>
      <% @orders.each do |orders| %>
        <tr>
          <td><%= link_to edit_admin_order_path(orders.id) do %>
              <%= orders.created_at.strftime("%Y/%m/%d %H:%M") %><% end %></td>
          <td><%= orders.order_details.sum(:quantity).to_i %></td>
          <td>
            <%= t("enums.order.status.#{orders.status}") %>
          </td>
        </tr>
      <% end %>
   </tbody>
  </table>
</div>
<div class = "mx-auto" ><%#= paginate @orders %></div>
