<div class="container takasa">
  <strong><%= notice %></strong>
  <div class="row d-flex">
    <!-- ジャンル検索サイドバー / パーシャル表示 -->
  	<%= render "public/homes/sideber", genres: @genres %>

    <div class="col-xs-9  align-items-center justify-content-center">
      <!-- 画像背景 -->
  		<div class="bg_top">
        <p class="text", style="font-size:30px">
            ようこそ、ながのCAKEへ！<br>
            このサイトは、ケーキ販売のECサイトになります。<br>
            会員ではない方も商品の閲覧はできますが、<br>
            購入には会員登録が必要になります。
        </p>
  		</div>
        <div class="col-xs-9">
          
      	</div>
      <!-- オススメ商品表示 -->
      <br>
      <div class="row">
  		  <h4 class="recommendation">新着商品</h4><br>
  		


        <div id="multiple-items">
        <% @new_items.each do |item| %>
          <div class="recommendation-items">
              <%= link_to item_path(item) do %> <!-- 画像をクリックしたら商品詳細画面へ遷移 -->
              <%= image_tag(item.get_item_image(300,200), alt: item.name) %><br>
            <% end %>
            <p><%= link_to item.name, item_path(item) %></p>
            <strong><%= link_to number_to_currency(item.price_excluding_tax) %></strong>
        <% end %>
　　　　　</div>
        </div>
      </div>
  	</div>

  	<div class="row">
  	  <div class="col-xs-9"></div>
  	  <div class="col-xs-2">
  	    <p><%= link_to "すべての商品を見る＞", items_path %></p>
  	  </div>
    </div>

  </div>
